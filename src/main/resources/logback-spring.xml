<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="ROOT_LOG_LEVEL" value="INFO"/>

    <include resource="logback-default-without-abac.xml"/>

    <!-- Spesiell håndtering av loggeren no.nav.log.LogFilter for å forhindre logging av isAlive-sjekker o.l. -->
    <appender name="stdout_json_with_filter" class="no.nav.common.log.MaskingAppender">
        <filter class="ch.qos.logback.core.filter.EvaluatorFilter">
            <evaluator>
                <matcher>
                    <Name>internal</Name>
                    <!-- Filtrerer ut isAlive og Prometheus-innslag i infologgen -->
                    <regex>status=200, method=GET, host=(.*), path=\/person\/personopplysninger-api\/internal(\/isAlive|\/actuator(.*))</regex>
                </matcher>

                <expression>internal.matches(formattedMessage)</expression>
            </evaluator>
            <OnMismatch>NEUTRAL</OnMismatch>
            <OnMatch>DENY</OnMatch>
        </filter>
        <appender name="stdout_json_masked_with_filter" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder" />
        </appender>
    </appender>

    <appender name="metrics" class="io.prometheus.client.logback.InstrumentedAppender"/>

    <logger name="no.nav.common.log.LogFilter" additivity="false">
        <!-- nais+local -->
        <appender-ref ref="stdout_json_with_filter"/>
        <appender-ref ref="metrics"/>
    </logger>
    <logger name="no.nav.sbl.featuretoggle.unleash" level="ERROR"/>
</configuration>
